import { Typography } from '@mui/material';

export const TripBookingPage = () => {
    return (
        <Typography component="h3" sx={{ typography: 'h5' }}>
            Trip Booking Details
        </Typography>
    );
};
