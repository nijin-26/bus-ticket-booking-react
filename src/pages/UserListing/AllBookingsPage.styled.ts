import { styled } from '@mui/material/styles';
import Box from '@mui/material/Box';

export const AllBookingsPageWrapper = styled(Box)(() => ({
    display: 'flex',
    flexDirection: 'column',
    height: '100%',
    overflow: 'hidden',
    width: '100%',
}));
